<div class="row">
  <div class="col-md-3">
    The Bookstore
    <!-- <div
  class="fb-like"
  data-share="true"
  data-width="450"
  data-show-faces="true">
    </div> -->
  </div>
  <div class="col-md-3">
    <%= link_to "HOME", root_path %> | <%= link_to "SHOP", books_path %>
  </div>
  <div class="col-md-3">
  <i class="fa fa-shopping-cart">
  CART(
    <% if current_user && current_user.orders %>
      <%= current_order.total_price %>
      $
    <% else %>
      EMPTY
    <% end %>
    )
  </i>
  <!--fa_icon "shopping-cart", text: "CART(EMPTY)"-->
  </div>
  <div class="col-md-3">
    <% if user_signed_in? %>
      <%= link_to "Settings", '#' %> |
      <%= link_to "Orders", '#' %> |
      <%= link_to 'Sign out', destroy_user_session_path, :method => :delete %>
    <%else%>
      <%= link_to 'Sign in', new_user_session_path %> |
      <%= link_to 'Sign up', new_user_registration_path %> |
      <%= link_to 'Sign in admin', new_admin_session_path %>
    <% end %>
  </div>
</div>